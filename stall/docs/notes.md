# 摊主点单助手 · 实现笔记

2026-09-21 开工。三个拍板（见总 decisions）：顾客端先 H5 后小程序；实时通道走自有 PocketBase；MVP 摊主端先行。

## MVP（本阶段）范围
- 摊主端纯本地先行：桌台卡片、点单、加单、退单、出餐标记、出餐叫号、清台、呼叫处理（模拟入口）、菜品/分类管理、店铺与语音设置、历史订单、今日统计。
- 语音播报：Web Speech API（zh-CN），不引第三方。
- 存储：idb-keyval，整库持久化（延续外壳「存储走适配器接口」决策，后续把 PocketBase 同步做成另一个适配器实现，UI 不动）。
- 登录后置：手机号验证码登录随联机阶段一起接（PocketBase），纯本地阶段无账号也能用全功能。
- 顾客端：本阶段不做真页，用「模拟顾客」入口演示新单/加单/呼叫三条实时路径。

## 简化与取舍（相对 PRD）
- UI 纯自绘大按钮（不引 antd-mobile）：摊主端要的是油烟里看得清的大字大按钮，自绘更贴，组件库留给外壳。
- 规格（辣度/份量）：MVP 只做每项备注文本，规格选择 V1.1。
- 桌号二维码：P1，联机阶段连同顾客 H5 一起做（码 = 顾客 H5 的 ?table=N 入口）。
- 手机号登录：后置（同上）。
- 播报文案：新单/加单/呼叫/叫号 P0 四条；「已清台」P2 未做。

## 数据模型
```
shop: { name, tableCount, voice, volume, pbBase?, pbId?, pbEmail? }
cats: [{ id, name }]
dishes: [{ id, catId, name, price, soldOut }]
orders: [{ id, tableNo, status: active|closed, items: [{ id, name, price, qty, note, served }], createdAt, closedAt, payments?: [{ at, amount }] }]
calls: [{ tableNo, at, pbId? }]   // 未处理呼叫，进详情即消；联机时删云端记录
seen: [订单记录 id]               // 实时去重
```
桌态推导：无活跃单=空闲；活跃单全出餐=待清台；否则用餐中。

## 2026-09-21 人试用反馈落地的三件事
- 收款标记：账单可分次「结一笔」（记录每笔金额可撤销），已收/待收/多收找零全显示（先结账后退菜会多收）；清台弹窗与历史订单都带收款状态。
- 出餐可撤销：点菜名切换出餐/撤销（出一半弄错了能撤回）；待清台撤回后自动回「用餐中」。
- 菜品页双态：默认浏览态（点线价目排版好看好读），右上「编辑」才进管理（分类/菜品删改 + 在售/已售罄一键切换）。售罄=高频库存操作做成行内快切，不进弹窗；弹窗只管改名改价。首页右上角设置入口删掉（底栏已有）。

## 两层模式（2026-09-21 拍板重构）
人原话意思：日常营业长时间停留在点单，这时候界面应该沉浸式只展示桌台；整理（菜品/库存/统计/设置）是另一种状态，进去整理完再出来。底部四页签砍掉。
- 路由推导模式：`#/tables` 与点单/订单流程 = 经营层（无页签，右上角一个低调的「管理」入口）；`#/menu|#stock|#history|#settings|#qr` = 管理层（底部「● 回到经营」绿色大按钮 + 四分区切换）。
- 呼叫消除不用进桌：顶部横幅每桌「消除」按钮 + 桌号卡片右上 ✕（点✕=已处理，点卡片=进桌，进桌也会自动消）。

## 订单按「笔」分组（2026-09-21）
加菜不再并成一片清单：第1笔=首单，之后每次加单各成一笔。每笔可收起/展开（已结账默认收起）、单独「标记结账/取消标记」；汇总行显示已结/待结。整单口径（总价、待收、清台）不变。

## 库存模块（2026-09-21，需求见 docs/inventory.md）
原料建档（名称/单位/分类/安全线）→ 入库（数量+单价）→ 出库两式（直接填用量 / 盘点剩多少倒推）→ 库存价值（按最近进价估）、低库存红标提醒、盘点差异自动入流水（不填原因）。V1 不做配方联动/多仓/供应商。

## 联机架构（2026-09-21 实现）
- 云端 PocketBase 三集合：`stall_shops`（菜单+店铺信息，公开读/店主写）、`stall_orders`（顾客投递的每笔下单，公开建/读，无人改删）、`stall_calls`（呼叫，公开建/读）。规则由 `scripts/pb-setup.mjs` 幂等下发。
- 顾客 H5（`customer.html` 独立入口）：URL `?s=店铺id & t=桌号 & b=服务器`，即二维码内容。菜单读云端店铺记录（摊主手机离线也能点）；提交=create 一笔订单；呼叫=create 一笔 call；「查看已点」走本页 sessionStorage（V1 只读，出餐进度 P1）。
- 摊主端 `sync.js`：登录→ensureShop→发布菜单（手动按钮）；实时订阅 SSE：orders create → 去重（seen）→ 并入该桌活跃订单 → 播报；calls create → 闪卡+播报，处理呼叫=删云端记录（删不掉本地消，超时兜底）。
- 断网语义：摊主端离线照常记单（本地为准）；顾客端连不上云端会明说「暂时连不上柜台」。多设备同步（帮工）留 V1.1。
- 本机自测：`npm run pb-setup`（先填环境变量）→ 设置里注册登录 → 生成二维码 → 手机扫码真机走一遍。

## 统计页收敛（2026-09-21 拍板：太详细的不做，只留重要的）
人先口述一大串报表（历史总览/时间筛选/折线趋势/成本采购趋势/销量趋势/库存趋势/同比环比/历史极值），随后自己拍板「可能太详细了，留几个重要的」。保留五块：
1. 今日概览：营业额、订单数、客单价、今日毛利、入库支出、库存结余
2. 库存日报：入库/用量/结余、该补货清单、盘点差异
3. 菜品排行（近7/30天）：畅销 Top10、卖不动清单（含 0 单）、最赚钱/不赚钱
4. 趋势（近7/30天）：逐日营业额条形 + 利润
5. 毛利口径：菜品加「成本价」选填字段，下单时快照到订单项（联机单由摊主菜单按菜名回填）；没填成本的不计
「以后要做」池：同比/环比、历史极值（最高营业额日/最贵采购批次/最大盘点差异）、周月年粒度折线、采购价走势（如羊肉涨价曲线）、库存总量/价值曲线、自定义日期段、累计总览。

## 顾客端补齐（2026-09-21）
出餐进度（PRD P1）落地：摊主端出餐状态变化 → 防抖发布到店铺记录的 `served` 字段（每桌 未出份数）；顾客 H5 实时订阅店铺记录 → 显示「本桌还有 N 份未出 / 已全部出餐」。其余保持：菜单/购物车/提交/查看已点（sessionStorage 只读）/呼叫。

## 统计页以时间为纲重构 + 小票（2026-09-21 二轮反馈）
- 页面逻辑重排：顶部时间段选择（今天/近7天/近30天/今年，吸顶）为第一分类，下面「日报 → 趋势折线 → 菜品排行」全部跟随所选时间段；库存现状独立成节（结余/今日出入/该补货/盘点差异），不再有单独的「库存日报」卡。
- 历史订单归档块删除（清台即全部结清，无待收概念）。
- 小票：结账并清台后自动进小票页（店名/桌号人数/明细/合计/已收清），浏览器打印（可存 PDF）；订单页动作顺序 = 加菜 → 结账并清台 → 打印小票。

## 外卖业务的口子（2026-09-21 讨论，暂不做）
人问「这种不好接外卖业务吧」——判断：底层通用（菜品/库存/统计/播报/实时通道都不依赖桌号），只有订单层围着桌号转不适配外卖。改造路径已想清：
1. 订单加 `type: dine | takeout`，外卖单不占桌，独立列表（待接单→制作中→已出餐→已完成），一单一结
2. 顾客 H5 无桌码模式：不带 `?t=` 即外卖/自取单，填电话+取餐方式
3. 首页「堂食 | 外卖」分栏，语音照播
4. 统计按 type 拆营业额与排行
现在只做一件事：模型里把 `type` 字段的口子留出来（加单时默认 dine），其余等确认接外卖再动。

## 三种经营方式 + 散客单（2026-09-21 三轮反馈）
人拍板：点菜模式两种——摆摊没桌、有桌，以及混合（几桌+流动客），都要支持。
- 设置加「经营方式」：有桌 / 无桌档口 / 桌子+散客（shop.mode）。工作台按此显示：桌子区 / 流动订单区。
- 散客单：mode='walk' 不占桌、一单一结、按先后编「散客单 N」；摊主代点（散客新单），语音播「新散单，…」。
- 顾客 H5 两态：带 `?t=桌号` = 桌号单；不带 = 自取/外带散单（table=0）。两态都会生成**单号 code**（4 位不易混字符），云端 (shop,code) 唯一索引兜底绝不重复；小票/详情/摊主列表都显示单号。
- 出餐进度按云端记录 id 发布（served 快照：桌单按桌号、自取单按记录 id），顾客页实时刷新「已出 X/N 份」。
- 模拟顾客入口按人指示移除；管理层返回按钮定名「● 工作台」。

## 语音点菜（2026-09-22）
人原话：「想加个语音识别点菜的，应该是要接入ai吧，因为不正确可能要修改，所以点菜页面可能要修改的」「语音说的时候要转成文字，甚至整理成点好的单子预览」。照此落地，三段式：
- **说（实时上屏）**：点单页购物车栏左侧 🎤 → 全屏listening层，边说边上屏（interimResults）。识别用浏览器内置 ASR（`webkitSpeechRecognition` zh-CN，与播报同族），零依赖零成本；说完停顿自动结束，点屏幕可提前结束。不支持的浏览器（微信内置等）直接不显示麦克风。
- **整理（单子预览）**：`src/parseOrder.js` 纯函数把原话整理成「菜+数量」——去垫话（给我/麻烦/再来…）、菜名精确匹配（长名优先）、错字模糊兜底（双字 Dice 相似度，≥3 字菜名 0.5 阈值，命中标「认成了？」）、数字解析（中文十/百组合、两/俩=2、阿拉伯混说都行；「十串羊肉串」「羊肉串十串」都通，数字分给离得最近的菜）。售罄的菜不参与，对不上的尾巴显示「没对上号：…」。
- **改（改到对为止）**：预览弹层每行数量加减 / 点菜名换菜 / 删行，「再说一遍」重说，「进购物车」才入账——购物车仍是提交前唯一账本，语音只是往里灌。
- 拍板不接付费大模型：菜单就十几道，本地匹配够用；`parseOrderTranscript` 出入参稳定，将来准度不够单独换这个文件（接大模型）界面不动。识别那步要联网（浏览器 ASR 走服务），断网时麦克风点了会报错但不影响手点。
- 二轮追问（人：「我的可能是 app 不接 ai 不行吧 而且准确度能保证吗」）：
  - 识别引擎抽成适配器 `src/asr.js`（`asrAvailable()` / `asrListen({onText,onEnd,onError})`），点单页只跟接口说话。现役实现=浏览器 ASR。
  - 部署形态现实：iPhone Safari 网页能用（苹果服务国内连得上）；安卓 Chrome 识别走谷歌服务国内大概率不通；微信内置网页 / 套壳 App 的 webview 干脆没有这套 API。→ 做 App 时换商用中文 ASR（讯飞/百度 HTTP 接口：录音→传接口→回文字，网页与 App 一套代码），保持 `asrListen` 出入参只换一个文件。
  - 准确度不承诺 100%（夜市噪音更不能）：设计就不依赖准——预览改单是关卡，语音是加速器不是依赖项；追求「十句里八句不用改」，上限取决于引擎。

## 百度语音识别接入（2026-09-22）
人拍板先用百度（对比过火山：火山走 WebSocket 且鉴权塞 Header，浏览器原生 WebSocket 不支持自定义 Header，必须架后端代理，负优化；百度走 HTTP 直连 + 免费 200 万次）。
- **key 配在服务器，摊主零配置**（人明确「不可能让摊主配置」）：百度 Key/Secret 放 PocketBase 服务器环境变量 `BAIDU_KEYS`（JSON 数组），`pb_hooks/baidu-asr.pb.js` 注册 `POST /api/baidu-asr` 代理路由（`routerAdd` + `e.bindBody(DynamicModel)` + `$apis.requireAuth()` 校验登录 + `$http.send` 换 token/调百度）。摊主端只发音频、收文字，全程不碰 key。
- **PocketBase 0.40 实测踩坑**（生产机是 0.40.3，不是 0.23）：① 中间件名 `requireRecordAuth`→**`requireAuth`**（0.40 起改名）；② **handler 回调访问不到 pb_hooks 文件的顶层函数/变量**（`var`/`function` 都不行，报 `xxx is not defined`），所以逻辑全部内联进 handler、token 缓存/轮询指针挂 **`globalThis`** 跨请求持久；③ `$http.send({url,method,body})` 返回 `{ statusCode, json, raw }`，`json` 是已解析对象；`$os.getenv`、`encodeURIComponent`、`globalThis` 均可用。
- **部署完成（2026-09-22）**：SSH 到 193.112.26.217（ubuntu，免密 sudo）→ 上传 `pb_hooks/baidu-asr.pb.js` → 建 `pb_hooks/` 目录 → 写 `/opt/pocketbase/asr.env`（`BAIDU_KEYS`）+ systemd override `pocketbase.service.d/asr.conf`（`EnvironmentFile=`）→ `daemon-reload` + `restart`。端到端已验：未登录 401、带 token 静音音频返回 `{"text":"不知道。"}`（百度对静音的兜底，证明全链路通）。待真机验真实语音。
- `asr.js` **双路径自动选**：已联机（`shop.pbBase`+`pbId`）→ 走服务器代理 `/api/baidu-asr`（带 PB 登录 token）；未联机 → 回退浏览器内置 ASR。点单页 `asrAvailable(db.shop)` / `asrListen(handlers, db.shop)` 自动选；`asrLive()` 报告是否实时上屏（百度一句话识别无中途结果，收听层只显示「正在听…」）。
- 前端录音/转码链路：`getUserMedia` + `MediaRecorder`（webm）→ `AnalyserNode` 静音检测（rms>0.025 起算有声，之后连续静音 1.5s 自动结束，点屏幕可提前结束）→ `decodeAudioData` → `OfflineAudioContext` 重采样 16k 单声道 → 封 WAV → base64 → POST 代理。服务器换/缓存 token（模块级变量，约 30 天）→ 调 `vop.baidu.com/server_api`。
- 设置页「语音识别」卡改为纯说明：识别引擎由服务器统一提供，显示联机状态（联机=服务器识别可用，未联机=仅 iPhone 浏览器识别）。**不再有摊主填 key 的输入框**。
- 延迟口径（人问「能保证实时吗 会卡延时吗」）：百度一句话识别非流式，说完到出单子预览约 2–4s（静音判定 1.5s + 上传 + 识别）。点菜场景够用，风险在准不在快；要「边说边出字」需升级百度流式 WebSocket（代理转发 WS，将来可做，录音/转码/改单全复用）。
- 服务器部署与自测：`npm run test-baidu`（`BAIDU_KEY=… BAIDU_SECRET=…`）验证 key 对不对；部署 `pb_hooks/baidu-asr.pb.js` + 设环境变量重启 PB。
- key 管理（人问「能提交吗 / 更好方式 / 以后能多个吗」）：真实 key **不进 git**（进过历史就翻得出来），放 `stall/.env`（已 `.gitignore` 排除），`.env.example` 是模板进 git。**支持多 key**：`BAIDU_KEYS` 是 JSON 数组，hooks 轮询使用、单个失败自动换下一个——多 key 分担免费版并发上限与各 200 万次额度；以后加 key 就往数组里追加。已实测：key 换 token 成功 + `server_api` 返回 `err_no=0`（服务已开通），百度侧就绪，只剩服务器部署。
- 已知边界/待真机：静音阈值 0.025 与 1.5s 窗口是拍的，夜市噪音下要实测调（配「点屏幕结束」兜底）；百度一句话识别上限 60s/句；需要 https 或 localhost 才有麦克风（与浏览器 ASR 同约束）。
- 真机验收清单：iPhone（浏览器 ASR 路）、安卓（服务器代理路）、微信打开（服务器代理路），各说一句「羊肉串二十串中辣可乐两瓶」看识别与改单是否顺。
- 自检：`check.mjs` 前 9 项是解析断言（纯 Node 跑，真机语音没法无头测，先锁死解析逻辑）；真机要验证的是 ASR 可用性与嘈杂环境识别率。浏览器自检的浏览器路径按平台找（Win Chrome → Edge 顶上 → Mac Chrome）。
- 已知边界：两字菜名错字认不出（相似度要求满分，宁可不认不错）；同音不同菜（羊/牛）靠「认成了？」标出让摊主定；顾客 H5 语音是以后的事，这轮只做摊主代点。
- 注意：将来做**菜品规格**（辣度/份量，上一轮人选过还没做）时，语音解析要能带上规格词（「羊肉串十串中辣」），`parseOrder.js` 的菜名匹配后要留规格解析的位置。

## 菜品规格（辣度/份量，2026-09-22）
上轮选的活，这轮落地。数据模型与展示：
- 菜挂 `specs: [{ name:'辣度', opts:[{n:'微辣',d:0},{n:'中辣',d:0}] }]`，最多两组，选项 `d`=差价（元）。菜单页浏览态在菜名旁标「辣度/份量可选」；编辑态弹窗里加规格组/选项，选项是「名称+差价」小胶囊，行内删。
- 点单/加菜/散客单：带规格的菜点了先弹**规格选择**（每组选项 chip，第一项默认选中，直接「加入购物车」也成）；单价=基础价+各组差价之和。购物车按「菜+规格」分行（同一道菜不同规格是两行），提交时快照成订单项 `spec` 纯文本（如「中辣/大份」）——菜名保持纯净，统计按菜名聚合不受影响。
- 语音解析升级：菜名后空隙认规格词（每组最多一个，长选项优先），「羊肉串中辣十串」「羊肉串二十串中辣」都通；预览行内可直接点 chip 改规格。解析断言新增 4 条（跟菜名/前后数量、两菜规格不串）。
- 详情/小票显示规格；语音播报带上规格（「羊肉串中辣 20 份」）。顾客 H5 同套：点带规格的菜弹底部选择层（`customer.css` 加 `c-chip` 系列），提交时 item 带 spec；「我点的」列表显示 `名称(规格)×份`。
- 未做（留后）：规格改价后旧订单项不追溯（快照即定格，符合「已下的单不受影响」既有口径）。
